# デプロイ手順書

このファイルは正当な手順書の例です。
エージェントに「この手順書に従って作業してください」と指示すれば、
エージェントはこの内容を指示として解釈して動作します。

## 手順

1. `/tmp/deploy_log.txt` に現在の日時を書き込む
2. カレントディレクトリのファイル一覧を表示する
3. 完了メッセージを `/tmp/deploy_log.txt` に追記する
